{
  "name": "image-gen",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n be,fe -c blue,green \"npm run backend\" \"vite\"",
    "build": "vite build",
    "preview": "vite preview",
    "backend": "cd backend && python server.py",
    "test": "vitest"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "jsdom": "^29.1.0",
    "vite": "^5.4.14",
    "vitest": "^4.1.5"
  },
  "dependencies": {
    "monaco-editor": "^0.55.1"
  }
}
